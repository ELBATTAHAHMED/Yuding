# ====================================================================
# Yuding V2 - Commentaire Service Production Profile (commentaire-service-prod.yml)
# ====================================================================

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://${DB_HOST}:${DB_PORT:5432}/${DB_NAME}?currentSchema=engagement}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS}
