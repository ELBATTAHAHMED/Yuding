# ====================================================================
# Yuding V2 - Reservation Service Staging Profile (reservation-service-staging.yml)
# ====================================================================

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://${DB_HOST:postgres}:${DB_PORT:5432}/${DB_NAME:yuding}?currentSchema=booking}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:https://staging.yuding.travel}
