# ====================================================================
# Yuding V2 - Reservation Service Production Profile (reservation-service-prod.yml)
# ====================================================================

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://${DB_HOST}:${DB_PORT:5432}/${DB_NAME}?currentSchema=booking}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS}
