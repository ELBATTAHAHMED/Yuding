package com.ahmed.travelservice.dto.response;

/** Whether a display conversion was obtained for a provider-backed amount. */
public enum ConversionStatus {
    CONVERTED,
    IDENTITY,
    UNAVAILABLE
}
